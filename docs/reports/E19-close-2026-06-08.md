# E19 Close Report — /pricing Page
**Date:** 2026-06-08
**Strategy executed:** 1 — Static Service Cards + Embedded Quote Calculator

---

## Files changed

| File | Action |
| :--- | :--- |
| `src/pages/PricingPage.tsx` | Created — full pricing page component |
| `src/App.tsx` | Modified — replaced PlaceholderPage at `/pricing` with `<PricingPage />` |
| `src/i18n/locales/en.json` | Modified — added `pricing.*` namespace (9 keys) |
| `src/i18n/locales/fr.json` | Modified — added `pricing.*` namespace (9 keys) |

---

## Page sections

1. **Hero** — H1 `pricing.hero.title`, subhead `pricing.hero.subtitle`. `bg-warm-white`.
2. **Service Pricing Cards** — 6 cards (Standard, Deep, Move-Out, Post-Construction, Airbnb, Commercial). Prices computed live from `calculateQuote('1-2bed', type, 'one-time')`. Reference note anchors expectation. `bg-cream`.
3. **Frequency Savings** — 3 cards (Weekly, Bi-weekly, Monthly) with save percentage and tagline. Reuses existing `quote.frequency.*`, `booking.fields.frequency.discounts.*`, and `recurring.tagline.*` keys — no new translation keys for this section. `bg-warm-white`.
4. **Embedded QuoteCalculator** — existing E06 component rendered inline; Travis can narrow to his exact price without leaving the page.
5. **CTA Strip** — `bg-slate-brand`, links to `/booking`.

---

## Persona tests

| Persona | Gate | Result |
| :--- | :--- | :--- |
| Travis (P2) | Price ranges visible above fold at 375px without interaction | **PASS** — service cards render immediately with computed price ranges; no contact info required |
| Margaret (P3) | 48px touch targets, 16px minimum text, no horizontal scroll at 768px | **PASS** — all CTAs use `min-h-[48px]`; body copy is `font-body text-base`; single-column layout at mobile |
| Diane (P1) | Full French UI — no hardcoded EN/FR strings | **PASS** — every visible string uses `t()`; `fr.json` `pricing.*` namespace complete |

---

## Subagent checks

- **Brand_Auditor:** All colour classes use token names; all buttons use `rounded` not `rounded-lg`; body copy is `font-body text-base`; all interactive elements `min-h-[48px]`; no v4 Tailwind syntax.
- **Data_Steward:** No Firestore reads or writes on this page. Pricing computed locally from `quotePricing.ts`.
- **Linguistic_Auditor:** All strings on the page use `t()`. The `pricing.*` namespace is complete in both `en.json` and `fr.json`. French copy reviewed for accuracy and tone.

---

## i18n keys added (new)

```
pricing.pageTitle
pricing.hero.title / pricing.hero.subtitle
pricing.services.heading / pricing.services.reference / pricing.services.commercial
pricing.frequency.heading / pricing.frequency.cta
pricing.cta.heading / pricing.cta.button
pricing.price.range
```

Reused existing keys (no new entries): `services.*.title`, `services.*.description`, `quote.frequency.*`, `booking.fields.frequency.discounts.*`, `recurring.tagline.*`, `recurring.mostPopular`, `common.bookNow`.

---

## Build gate

```
npm run build  → clean (0 errors)
npm run lint   → clean (0 warnings)
```
