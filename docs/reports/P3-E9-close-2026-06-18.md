# P3-E9 Close Report — Remove `window.__MOCK_*` from Production
**Date:** 2026-06-18  
**Epic:** P3-E9 — Remove `window.__MOCK_*` from Production  
**Persona:** Travis McLeod (P2) — clean production path; no test scaffolding in live bundle

---

## Summary

Removed the `window.__MOCK_SUBMIT__` production guard from `firestore.ts` and migrated E2E tests to use Playwright `page.route()` network interception. The production bundle no longer contains any `window.__MOCK_*` references.

---

## Changes Made

### Production Code
- **`apps/customer/src/lib/firebase/firestore.ts`** — Removed `window.__MOCK_SUBMIT__` guard from `submitBooking()`. The function now goes directly to Firestore on every call.
- **`apps/customer/src/vite-env.d.ts`** — Removed `Window` interface extension that declared `__MOCK_SUBMIT__` and `__MOCK_CREATE_PAYMENT_INTENT__`.

### E2E Tests
- **`apps/customer/e2e/booking.spec.ts`** — Rewrote `beforeEach` to use `page.route('**/firestore.googleapis.com/**', ...)` instead of `window.__MOCK_SUBMIT__`. Mock response format: `{ writeResults: [{ updateTime: "..." }], status: [] }`.
- **`apps/customer/e2e/checkout.spec.ts`** — Replaced `addInitScript` window mock with `page.route()` for Firestore. Retained `window.Stripe` and `window.__MOCK_CREATE_PAYMENT_INTENT__` mocks (deferred to P3-E1 when Stripe ships). Added comment documenting the deferred migration path.

---

## Strategy Chosen
**Strategy A** (approved via grill-me): Migrate the Firestore half of checkout.spec.ts; leave Stripe window inject intact until P3-E1 ships.

---

## Persona Test
- **Travis (P2):** `submitBooking()` in production no longer branches on `window.__MOCK_SUBMIT__`. Travis's booking flows directly to Firestore with no test scaffolding in the bundle. ✅

---

## Subagent Audits
- **Data_Steward:** No Firestore schema changes. `submitBooking()` writes identical fields post-removal. ✅
- **Linguistic_Auditor:** No UI string changes. ✅
- **Brand_Auditor:** No Tailwind class changes. ✅

---

## Build
`npm run build` — customer and FSM apps both pass. ✅

---

## Deferred
- `window.__MOCK_CREATE_PAYMENT_INTENT__` in `checkout.spec.ts` — migrate to `page.route()` on the Cloud Functions callable endpoint when P3-E1 (Stripe) ships.
