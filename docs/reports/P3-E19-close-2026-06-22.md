# P3-E19 Close Report — 2026-06-22

**Epic:** Cloud Functions Domain Split  
**Band:** D · Sprint 5  
**Status:** ✅ Completed

## Work Done

1. **Bootstrap Centralization (`lib/shared.ts`)**:
   - Centralized Firebase Admin `initializeApp()` and `Sentry.init(...)` logic.
   - Defined and exported all Twilio, Resend, and Admin email environment secrets.
   - Exported Firebase instances (`db`, `auth`) and `logError` helpers to ensure error metrics consistently propagate to Sentry.

2. **Domain-Scoped Modules**:
   - Refactored `functions/src/index.ts` from a ~1,480-line monolithic file down to a ~15-line index of pure re-exports.
   - Distributed all 19 Cloud Functions into subdirectories (`triggers/`, `callable/`, `scheduled/`).
   - Split job triggers into `job.created.ts` and `job.updated.ts` to enforce the strict 200-line maximum file length guideline.

3. **Ignore Configuration Fix**:
   - Modified `functions/.gitignore` to anchor the compiler ignore rule `/lib/`, ensuring `/functions/src/lib/shared.ts` is correctly tracked by Git while keeping only compiled output `/functions/lib/` ignored.

## Function Destination Mapping

| Function | Type | Destination File |
|---|---|---|
| `onBookingCreated` | Firestore Trigger | `triggers/booking.ts` |
| `onBookingStatusConfirmed` | Firestore Trigger | `triggers/booking.ts` |
| `onBookingCancelled` | Firestore Trigger | `triggers/booking.ts` |
| `onJobCreatedTrigger` | Firestore Trigger | `triggers/job.created.ts` |
| `onJobUpdatedTrigger` | Firestore Trigger | `triggers/job.updated.ts` |
| `onJobStatusCompleted` | Firestore Trigger | `triggers/job.updated.ts` |
| `onStaffUpdatedTrigger` | Firestore Trigger | `triggers/staff.ts` |
| `onUserCreated` | Auth Trigger (v1) | `triggers/auth.ts` |
| `setUserRole` | Callable | `callable/auth.ts` |
| `claimJob` | Callable | `callable/job.ts` |
| `getAnalyticsKPIs` | Callable | `callable/analytics.ts` |
| `onStaffRegistered` | Callable | `callable/staff.ts` |
| `migrateComplianceRecords` | Callable | `callable/staff.ts` |
| `createPaymentIntent` | Callable | `callable/payments.ts` |
| `stripeWebhookHandler` | Request (HTTPS) | `callable/payments.ts` |
| `onDailyReminderCheck` | Scheduled Cron | `scheduled/reminders.ts` |
| `onDailyRecurringRenewal` | Scheduled Cron | `scheduled/renewals.ts` |
| `onMonthlyEarningsRollover` | Scheduled Cron | `scheduled/earnings.ts` |
| `onReviewEmailScheduler` | Scheduled Cron | `scheduled/reviews.ts` |

## Build & Deployment Verification

- `npm run build` in `functions/` completed successfully with zero compile warnings/errors. ✅
- Deployed all 19 functions successfully to `freshnest-aa51e` using `npx firebase deploy --only functions`. ✅
- Root compilation (`npm run build`) completed successfully, ensuring the `customer` and `fsm` applications compile cleanly with the updated backend exports. ✅

## Persona Test

- **P12 Lauren (Admin)**: Operational reliability is maintained. Centralized Sentry logging ensures any future runtime errors are immediately captured and diagnosed.
- **P8 Jasmine (Mobile Staff) / P11 Brenda (Lead Cleaner)**: Relies on scheduling notifications. Trigger moves did not change or delay the delivery of job confirmations, rollover status updates, check-in notifications, or daily scheduling SMS alerts.
- **P2 Travis (Fast Booking)**: All booking-related triggers and creation checks execute identically with no runtime delay or logic alterations.

## Subagent Audits
- **Brand_Auditor**: **PASS**. Verified that no user-interface changes, styling modifications, or Tailwind regressions were introduced in this backend-only refactor. Email templates were verified to align with design-system color standard tokens.
- **Data_Steward**: **PASS**. Confirmed that all Firestore queries, reads, updates, and writes match the existing schema formats defined in `docs/firestore-schema.md` and no invented fields or collections were introduced.
- **Linguistic_Auditor**: **PASS**. Verified that all API endpoints, background schedulers, SMS systems, and logging mechanisms use localized or language-specific variables, and all internal errors match strict logger formats.
