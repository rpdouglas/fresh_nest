# P3-E27-B1 Close Report — 2026-06-22

**Epic:** Automated Welcome Email with Magic Link  
**Band:** C (Employee Onboarding sub-phase) · Sprint 6  
**Status:** ✅ Completed

## Work Done

1. **Shared Types and Schema Mapping**:
   - Added `welcomeEmailSentAt?: Date | null` to the `Staff` type definition in `packages/shared/src/types/staff.ts`.
   - Updated the canonical Firestore `staffConverter` in `packages/shared/src/firebase/converters.ts` to map `welcomeEmailSentAt` timestamps from Firestore to Javascript Date objects during document reads.
   - Updated `docs/firestore-schema.md` to document the new schema field.

2. **Cloud Functions Extension**:
   - Defined the `FSM_APP_URL` secret configuration parameter via `defineSecret` in `functions/src/lib/shared.ts` to support redirect link mapping in both emulator and production environments.
   - Extended the `onStaffRegistered` registration callable function inside `functions/src/callable/staff.ts` to generate a sign-in magic link with redirect URL `${FSM_APP_URL}/login?onboarding=true` (handling local port fallbacks) and trigger the Resend email dispatch.
   - Implemented a dedicated `resendWelcomeEmail` Cloud Function in `functions/src/callable/staff.ts` that enforces administrative authorization, fetches the staff profile, generates a new magic link, sends the welcome email, and updates `welcomeEmailSentAt` timestamp in Firestore.
   - Exported the new function in `functions/src/index.ts`.

3. **Branded Email Templates & Dispatch**:
   - Defined `staffWelcomeEn` and `staffWelcomeFr` in `functions/src/emailTemplates.ts` using brand-aligned styling (slate-brand `#5b7e8f` header, cream background, clean typography). Email body includes personalized greeting, onboarding details (background check and Terms acceptance expectations on first login), CTA button, Lauren's contact details, and brand footer.
   - Created the `sendWelcomeEmail` wrapper inside `functions/src/sendEmail.ts` utilizing the `Resend` SDK.

4. **Admin UI Directory Integration**:
   - Updated `apps/customer/src/components/admin/hooks/useStaff.ts` to export a `resendWelcome(uid)` helper method that invalidates staff queries upon success.
   - Integrated unit tests for `resendWelcome` inside `apps/customer/src/components/admin/hooks/useStaff.test.ts`.
   - Modified `apps/customer/src/components/admin/StaffTable.tsx` to render the sent date status in the "Contact" column, and added a "Resend Invite" action button next to the "Export" button in the "Actions" column. Managed loading/resending and error toast alerts.

## Build & Test Verification

- `npm run build` at root: **PASS** (Both client apps and packages compile successfully). ✅
- `npm run build` in functions/: **PASS** (Tsc compiler succeeds with no type checking errors). ✅
- `npm run test:customer`: **PASS** (All 39 unit tests pass successfully, including useStaff resend assertions). ✅
- `npm run test:fsm`: **PASS** (All 50 unit tests pass successfully). ✅
- `npm run lint`: **PASS** (Ecosystem linter runs with no errors on modified files). ✅

## Persona Test

- **P12 Lauren Arsenault (Admin)**: Can monitor staff invite statuses directly from the directory list. Resending invites is automated via a single click in the actions column. Errors are caught and logged to Sentry while displaying a helpful toast in the UI.
- **P8 Jasmine Beausoleil (Cleaner)**: Receives a clean, branded email containing a CTA button with an embedded magic link that logs her in automatically and triggers the first-login consent sequence.
- **P11 Brenda Côté (Lead Cleaner)**: Receives her welcome email and onboarding instructions completely in French according to her language choice.

## Subagent Audits
- **Brand_Auditor**: **PASS**. Tested email styling matches slate-brand color guidelines. Toast alerts in `StaffTable` reuse pre-configured Tailwind classes.
- **Data_Steward**: **PASS**. Firestore write payload `welcomeEmailSentAt` conforms to schema rules, and `toFirestore` strips undefined fields.
- **Linguistic_Auditor**: **PASS**. Translation tokens added to `StaffTable.tsx` dynamically load localized translations, ensuring no hardcoded strings.
