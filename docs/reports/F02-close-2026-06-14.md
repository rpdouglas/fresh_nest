# F02 Close Report — Staff Profile & Settings
**Date:** 2026-06-14  
**Epic:** F02 | **Phase:** FSM Phase 2 — Staff Foundation  
**Strategy Executed:** Strategy 1 — Consolidated Admin Tab & Visual FSM Profile Dashboard  
**Build:** ✅ Clean | **Lint:** ✅ Clean | **Tests:** ✅ 33/33 passed

---

## Summary

Epic F02 implements the staff self-service profile dashboard in the FSM portal and the admin-side Staff Registry in the Customer App. It targets four personas: Carla (P7 — ODSP cap), Jasmine (P8 — transit buffers), Mike (P9 — blocked windows), and Sarah (P12 — staff registration admin).

---

## Files Created

| File | Purpose |
| :--- | :--- |
| `apps/fsm/src/pages/ProfilePage.tsx` | FSM profile dashboard: personal info, transport, blocked windows, ODSP gauge |
| `apps/fsm/src/pages/ProfilePage.test.tsx` | 5 unit tests covering all visual components and save mutation |
| `apps/customer/src/components/admin/StaffTable.tsx` | Staff registry table with search, role/status filters, register button |
| `apps/customer/src/components/admin/RegisterStaffModal.tsx` | Modal form to create `/staff` documents; validates with Zod + React Hook Form |

## Files Modified

| File | Change |
| :--- | :--- |
| `apps/customer/src/pages/AdminPage.tsx` | Added `'staff'` tab state, Staff tab button, and `<StaffTable>` rendering |
| `apps/fsm/src/App.tsx` | Added `/profile` route wired to `<ProfilePage />` behind `ProtectedRoute` |
| `apps/fsm/src/components/layout/FsmLayout.tsx` | Added "Profile" nav link to sidebar and mobile menu |
| `apps/fsm/src/i18n/locales/en.json` | Added `fsm.profile`, `fsm.profile.transport`, `fsm.profile.blockedWindows`, `fsm.profile.odsp` keys |
| `apps/fsm/src/i18n/locales/fr.json` | Complete French translations for all new keys |
| `apps/customer/src/i18n/locales/en.json` | Added `admin.staff.*` and `admin.staff.modal.{roleOption,statusOption,transportOption}` keys |
| `apps/customer/src/i18n/locales/fr.json` | Complete French translations for all new admin.staff keys |

---

## Persona Acceptance Gate

| Persona | Test | Result |
| :--- | :--- | :--- |
| **Carla (P7)** | ODSP gauge renders at 75% ($750/$1000), amber caution state with correct message in EN and FR | ✅ PASS |
| **Jasmine (P8)** | Transport mode "transit" shows transit buffer field; updates persist to `constraints.transitBufferMinutes` | ✅ PASS |
| **Mike (P9)** | Blocked windows manager: add/remove recurring slots with private labels; unit tests confirm flows | ✅ PASS |
| **Sarah (P12)** | Staff Registry tab lists staff with role/status badges; Register New Staff modal saves full `/staff` schema doc | ✅ PASS |

---

## Schema Compliance (Data_Steward Audit)

All Firestore writes comply with `/staff` collection schema in `docs/firestore-schema.md`:

- `RegisterStaffModal` writes to `/staff/{uid}` with correct top-level fields: `firstName`, `lastName`, `email`, `phone`, `role`, `status`, `preferences.language`, `constraints.transportMode`, `constraints.transitBufferMinutes`, `financials.monthlyEarningsLimit`, `onboardingChecklist`, `createdAt`
- `ProfilePage` updates use dot-notation paths: `constraints.transportMode`, `constraints.transitBufferMinutes`, `constraints.blockedWindows`, `financials.monthlyEarningsLimit` — never overwrites unrelated sub-fields

No invented Firestore fields. ✅

---

## Brand Compliance (Brand_Auditor)

All Tailwind classes use tokens from `docs/design-system.md`:
- Colors: `slate-brand`, `slate-dark`, `slate-pale`, `cream`, `warm-white`, `sand`, `charcoal`, `text-muted`
- Fonts: `font-display`, `font-sub`, `font-body`
- Border radius: `rounded` (4px brand standard)
- Touch targets: `min-h-[48px]` on all interactive elements (Margaret/P3 compliance)
- Font sizes: `text-base` minimum (16px) throughout (Margaret/P3 compliance)

✅ Zero deviations from design system.

---

## Linguistic Compliance (Linguistic_Auditor)

- All UI strings in `ProfilePage.tsx`, `StaffTable.tsx`, `RegisterStaffModal.tsx` use `t()` hook
- Zero hardcoded English or French strings in component JSX
- All new i18n keys exist in both `en.json` and `fr.json`
- `fsm.profile.saving` used as button label during save (bilingual)
- ODSP safety messages (`safe`/`caution`/`at_limit`) fully translated in both languages

✅ Linguistic compliance confirmed.

---

## Test Results

```
FSM Tests:  3 files, 21 tests — ✅ all passed
  - ProfilePage.test.tsx (5 tests)
  - LoginPage.test.tsx (7 tests)
  - useStaffAuth.test.tsx (9 tests)

Customer Tests: 2 files, 12 tests — ✅ all passed
  - analytics.test.ts (8 tests)
  - booking-form-validation.test.ts (4 tests)

Total: 33 tests, 0 failures
```

---

## Checklist Verification

- [x] i18n keys in `apps/customer/src/i18n/locales/` for staff list and registration modal
- [x] i18n keys in `apps/fsm/src/i18n/locales/` for profile constraints and ODSP visuals
- [x] `StaffTable.tsx` created for staff registry listing
- [x] `RegisterStaffModal.tsx` created for staff enrollment form
- [x] "Staff" tab added to `AdminPage.tsx` with table and modal integration
- [x] `ProfilePage.tsx` created with transport controls, blocked windows manager, ODSP gauge
- [x] ProfilePage mounted in `App.tsx` and linked in `FsmLayout` navbar
- [x] Unit tests in `ProfilePage.test.tsx` (5 tests) — all pass
- [x] Brand_Auditor — no deviations
- [x] Data_Steward — no invented fields
- [x] Linguistic_Auditor — all strings via t()
- [x] `npm run build` — ✅ clean
- [x] `npm run lint` — ✅ clean
- [x] All test suites — ✅ 33/33 passing
