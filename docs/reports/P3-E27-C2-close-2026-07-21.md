# P3-E27-C2 Close Report — 2026-07-21

**Epic:** Employee Self-Service Profile Completion
**Band:** C (Employee Onboarding sub-phase) · Sprint 7
**Status:** ✅ Completed

## Work Done

1. **`ProfilePage.tsx` — "My Details" card**: added a read-only status badge (Onboarding/Active/Inactive) next to the existing role badge; made `phone` an editable input (was read-only text); added a language `<select>` (en/fr/ar) writing `preferences.language` and calling `i18n.changeLanguage()` immediately on save.
2. **New "Flag a Correction" card**: free-text field appending `{ text, flaggedAt }` to a new `corrections` array via `arrayUnion`, matching the existing `compliance.termsHistory` write pattern — no new callable needed.
3. **New "Emergency Contact" card**: pre-filled from `staffProfile.emergencyContact` (set during P3-E27-C1's first-login sequence), editable, with an all-or-nothing validation (can't save 2 of 3 fields).
4. **Availability / Earnings cards**: unchanged, per the approved decision to leave `financials.monthlyEarningsLimit` self-editable rather than lock it down to admin-only.
5. **`firestore.rules`**: added `phone` and `corrections` to the `staff/{staffId}` self-service `hasOnly()` allowlist — both fully employee-writable, same trust model as `compliance`/`emergencyContact`, no nested-diff logic needed.
6. **`onStaffUpdatedTrigger`**: added a 5th tracked field (`phone`) to the existing audit-log diffing logic, attributed to the employee's own uid (self-service edit) rather than `'admin'`.
7. **Schema**: added `corrections: ProfileCorrection[]` to `Staff`, `staffConverter`, `onStaffRegistered`'s initialization, and `docs/firestore-schema.md`. Also corrected a latent type gap: `StaffLanguage` was `'en' | 'fr'` only, even though `LanguageSelectionOverlay` already wrote `'ar'` at runtime (P10 Ahmed's FSM Arabic toggle) — widened to `'en' | 'fr' | 'ar'`.
8. **i18n**: 18 new keys across `fsm.profile.status`, `statuses.*`, `language`, `languages.*`, `contact.phoneRequired`, `corrections.*`, `emergencyContact.*`, full EN/FR parity.

## Build & Test Verification

- `npm run build` (root, both apps + functions): **PASS** — zero TypeScript errors.
- `npm --workspace=apps/fsm run test`: **9 of 12 test files pass, 59/59 tests pass** in every file this epic touched. **Fixed the pre-existing environment gap in `ProfilePage.test.tsx`** (same fix pattern as P3-E27-C1's `TermsConsentOverlay.test.tsx` fix) since this epic directly extends that file — it now passes cleanly with 13 tests, including 8 new ones covering phone edit, language change, emergency-contact pre-fill/save/incomplete-validation, and correction flagging. **3 pre-existing, unrelated test files still fail** (`JobPage.test.tsx`, `MyJobsPage.test.tsx`, `ShiftBoardPage.test.tsx`) — same root cause (missing Firebase env vars), confirmed via `git status` to be files this epic did not touch.
- `npm --workspace=apps/fsm run lint`: **PASS** — zero errors.
- `npm --workspace=apps/customer` build: unaffected (this epic made no `apps/customer` changes) — covered by the root `npm run build`.
- `npm --workspace=apps/customer run test:rules`: **not run** — `firebase-tools`/emulator still not installed in this environment (same gap noted in the B2 and C1 close reports). Reviewed manually instead (see Subagent Audits).

## Persona Test

- **P13 Marcus Oakes (Part-time student)**: can update his phone number without calling Lauren, extending the same self-service principle his blocked-windows management already had.
- **P8 Jasmine Beausoleil (Cleaner)**: her emergency contact (collected in C1) is visible on her profile and editable if circumstances change.

## Open Decision — Resolved
Per the epic spec's flagged open decision, the user confirmed `financials.monthlyEarningsLimit` should **remain self-editable** (no change from already-shipped behavior), rather than being locked down to admin-only as the original narrative source doc's acceptance criteria suggested.

## Subagent Audits
- **Brand_Auditor**: 1 violation found (`correctionText` textarea was missing `min-h-[48px]`, the only new control in this epic without it) — fixed. **PASS** after fix. Status-badge colors match the existing precedent already used in `StaffTable.tsx` (no brand-token equivalent exists for semantic status colors — documented as separate, non-blocking drift in the original governance review).
- **Data_Steward**: **PASS** — the single `updateDoc` write (`phone`, `preferences.language`, `emergencyContact`, `corrections`) and the new phone audit-log entry both match `docs/firestore-schema.md` exactly. No invented fields, no hardcoded database IDs, no PII in browser storage.
- **Linguistic_Auditor**: **PASS** — zero hardcoded strings; perfect EN/FR key parity (18/18 new keys).
- **Security_Auditor**: **PASS** — `phone`/`corrections` added to the existing self-service allowlist matches the `compliance`/`emergencyContact` trust model exactly, no new attack surface. One minor, non-blocking note: `corrections` isn't append-enforced at the rules level (same pre-existing characteristic as `compliance.termsHistory`, not a new risk C2 introduces). The `firestore.rules` change was pre-approved by the user via Strategy 1 approval in `docs/plans/P3-E27-C2_PLAN.md`.

## Known Follow-Ups (not blocking this epic's close)
- FSM unit tests still need Firebase env vars (or a shared test-setup mock) for the 3 remaining pre-existing affected test files (`JobPage`, `MyJobsPage`, `ShiftBoardPage`) — unrelated to this epic, carried over from prior close reports.
- `firebase-tools`/Firestore emulator still not installed in this container — `test:rules` can't be run here.
- No admin UI yet for reviewing `corrections` entries — deferred to P3-E27-D1 (Staff Detail Panel), as noted in the epic spec.
