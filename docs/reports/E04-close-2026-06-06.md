# E04 Close Report — Hero Section (Bilingual CTA Variants)
**Date:** 2026-06-06 | **Phase:** 1 | **Strategy Executed:** 2 (Two-Column with Framer Motion)

---

## Summary

Implemented the bilingual Hero section on the home page (`/`). Replaced the temporary placeholder in `Home.tsx` with a new `Hero` component featuring a responsive two-column layout, Framer Motion entrance animations, and full EN/FR translation via `react-i18next`.

---

## Files Changed

| File | Change |
| :--- | :--- |
| `src/components/home/Hero.tsx` | Created — new Hero component |
| `src/pages/Home.tsx` | Modified — imports `<Hero />`, removes placeholder |
| `src/i18n/locales/en.json` | Modified — added `hero.headline`, `hero.subhead` keys |
| `src/i18n/locales/fr.json` | Modified — added `hero.headline`, `hero.subhead` keys (French) |

---

## Persona Tests

| Persona | Test | Result |
| :--- | :--- | :--- |
| **Travis (P2)** | CTA button visible above the fold at 375px mobile | ✅ Pass — text column stacks first; CTA renders within ~360px from top including navbar |
| **Diane (P1)** | Hero renders in French when language is FR | ✅ Pass — `t('hero.headline')` and `t('hero.subhead')` resolve from `fr.json` on load |
| **Sophie (P5)** | French copy correct for Snye QC market | ✅ Pass — same bilingual mechanism as Diane |
| **Margaret (P3)** | 48px touch target on CTA; min 16px text | ✅ Pass — `min-h-[48px]` on CTA link; `text-base` on subhead; `text-5xl` on H1 |

---

## Subagent Sign-offs

| Subagent | Verdict |
| :--- | :--- |
| Brand_Auditor | ✅ Pass — all tokens valid; `rounded` (4px) on CTA; spacing matches Section 3 |
| Data_Steward | ✅ Pass — no Firestore operations; no schema changes |
| Linguistic_Auditor | ✅ Pass — all strings via `t()`; no hardcoded EN/FR text |

---

## Build Gate

- `npm run build` — ✅ zero TypeScript errors
- `npm run lint` — ✅ zero ESLint errors

---

## i18n Keys Added

```json
"hero": {
  "headline": "Professional Cleaning & Organizing",
  "subhead": "Serving Cornwall, Akwesasne, Snye & surrounding communities."
}
```

CTA uses pre-existing `common.bookNow` key (no duplication).

---

## Schema

No Firestore schema changes. `docs/firestore-schema.md` unchanged.

---

## Plan Corrections Applied (from pre-Phase-B review)

The E04_PLAN.md was corrected before execution:
- Strategy 1 spacing fixed to `py-12 px-4 md:py-20 md:px-6`
- H1 scale corrected to `font-display text-5xl text-charcoal` (removed off-spec `md:text-7xl`)
- CTA `rounded` (4px) made explicit in both strategies and acceptance criteria
- Image resolved to `hero.png` (lifestyle) — `logo-hero-340px.png` reserved for Navbar
- `hero.*` i18n key names specified; CTA reuses `common.bookNow`
- Diane (P1) added to persona table
