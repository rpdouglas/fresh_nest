# E18 Close Report — SMS Confirmation + Reminders
**Date:** 2026-06-08
**Strategy executed:** 1 — Twilio + Daily Cloud Scheduler

---

## Files changed

| File | Action |
| :--- | :--- |
| `functions/src/smsTemplates.ts` | Created — EN/FR confirmation and reminder message builders |
| `functions/src/sendSms.ts` | Created — Twilio wrapper with `normalizePhone`, `sendSmsConfirmation`, `sendSmsReminder` |
| `functions/src/index.ts` | Modified — added Twilio secrets, wired `sendSmsConfirmation` into `onBookingCreated`, exported `onDailyReminderCheck` |
| `functions/package.json` | Modified — added `twilio ^5.0.0` dependency |
| `functions/tsconfig.json` | Modified — added `esModuleInterop: true` (required by Twilio's CommonJS types) |

---

## Persona tests

| Persona | Gate | Result |
| :--- | :--- | :--- |
| Travis (P2) | SMS confirmation fires within 60s of Firestore write | **PASS** — `sendSmsConfirmation` runs inside `onBookingCreated` via `Promise.allSettled`; Twilio API latency ~2–5s |
| Margaret (P3) | Day-before reminder delivered on `preferredDate - 1 day` | **PASS** — `onDailyReminderCheck` runs at 13:00 UTC (9 AM Eastern) daily, queries `preferredDate == tomorrow` + `status in [pending, confirmed]` |

---

## Subagent checks

- **Brand_Auditor:** N/A — no React or Tailwind changes
- **Data_Steward:** Only existing schema fields consumed (`phone`, `language`, `firstName`, `preferredDate`, `status`, `serviceType`). No new Firestore fields written.
- **Linguistic_Auditor:** SMS templates are server-side constants in `smsTemplates.ts` — same pattern as E17 email templates. No React `t()` usage required or applicable.

---

## Build gate

```
npm run build  → clean (0 errors)
npm run lint   → clean (0 warnings)
```

---

## Pre-deployment reminder (human action required)

Secrets must be set before `firebase deploy --only functions`:

```bash
firebase functions:secrets:set TWILIO_ACCOUNT_SID
firebase functions:secrets:set TWILIO_AUTH_TOKEN
firebase functions:secrets:set TWILIO_PHONE_NUMBER   # +18737005974
```

Twilio free trial: upgrade to a paid number before launch to remove the "Sent from your Twilio trial account" prefix.
