# P3-E18 Close Report — 2026-06-18

**Epic:** Shared Types Package  
**Band:** A · Sprint 1  
**Status:** ✅ Completed

## Work Done

Created canonical shared type files in `@freshnest/shared`:

| File | Key Types |
|---|---|
| `packages/shared/src/types/booking.ts` | `Booking`, `BookingStatus`, `ServiceType`, `Frequency`, `LeadSource` |
| `packages/shared/src/types/staff.ts` | `Staff`, `StaffRole`, `StaffStatus`, `BlockedWindow`, `TermsAcceptance` |
| `packages/shared/src/types/job.ts` | `Job`, `JobPhoto` (lat/lng), `JobStatus`, `PayRateSnapshot` |
| `packages/shared/src/types/common.ts` | `ChecklistTask` (labelEn/labelFr), `ChecklistTemplate`, `PayRate`, `AuditEntry` |

Both apps' `types/index.ts` rewritten to re-export from `@freshnest/shared`, keeping only app-local types locally.

## Type Divergences Resolved

| Field | Resolution |
|---|---|
| `ChecklistTask.labelKey` (FSM) | Replaced with `labelEn/labelFr` canonical fields; `JobPage.tsx` updated to render directly |
| `JobPhoto.geoLat/geoLng/geoTagged` (FSM) | Replaced with `lat?/lng?`; `OfflineUploadProvider.tsx` updated |
| `Job.id` required (FSM) | Now `id?: string` in canonical; added `if (!job.id) return` guards in `JobPage.tsx` |
| `Staff.uid` required (FSM) | Now `uid?: string` in canonical; added `if (!staffProfile.uid) return` guards |

**Intentionally excluded from shared types:** `QueuedPhoto` in `apps/fsm/src/lib/utils/indexedDb.ts` — IndexedDB-specific internal schema, retains geoLat/geoLng/geoTagged.

## Build Verification
`npm run build` — both customer and FSM apps build with zero TypeScript errors. ✅  
`npm run build` in `functions/` — zero errors. ✅

## Persona Test
P2 Travis, P6 Gallagher — booking and job photo data now share canonical types, preventing silent type drift between the two apps.

## Subagent Checks
- Brand_Auditor: N/A (no UI changes)
- Data_Steward: N/A (no Firestore schema change — types only)
- Linguistic_Auditor: N/A (no new UI strings)
